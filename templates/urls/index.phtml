<div class="container-lg mt-3">
    <h1>Список страниц</h1>

    <?php if (isset($_SESSION['flash'])): ?>
        <div class="alert alert-<?= $_SESSION['flash']['type'] ?> alert-dismissible fade show" role="alert">
            <?= htmlspecialchars($_SESSION['flash']['message']) ?>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <?php unset($_SESSION['flash']); ?>
    <?php endif; ?>

    <table class="table table-bordered table-hover mt-3" data-test="urls">
        <thead>
        <tr>
            <th>ID</th>
            <th>Имя</th>
            <th>Последняя проверка</th>
            <th>Код ответа</th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($urls ?? [] as $url): ?>
            <tr>
                <td><?= htmlspecialchars($url['id'] ?? '') ?></td>
                <td>
                    <a href="/urls/<?= htmlspecialchars($url['id'] ?? '') ?>">
                        <?= htmlspecialchars($url['name'] ?? '') ?>
                    </a>
                </td>
                <td><?= htmlspecialchars($url['last_check']['created_at'] ?? '') ?></td>
                <td><?= htmlspecialchars($url['last_check']['status_code'] ?? '') ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>